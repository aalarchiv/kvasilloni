
# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key

# reference projects, docs, etc that must not get into repo
refs/

# Rust / cargo build output
/target/

# C prototype build output
/reference/c-prototype/build/
